<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".sclad.ScladActivity"></activity>
        <activity android:name=".sale.SaleActivity" />
        <activity android:name=".receiving.AddReceivingActivity" />
        <activity android:name=".receiving.ReceivingActivity" />
        <activity android:name=".partners.AddPartnerActivity" />
        <activity android:name=".partners.PartnerActivity" />
        <activity android:name=".goods.AddGoodsActivity" />
        <activity android:name=".goods.GoodsActivity" />
        <activity android:name=".sale.AddSaleActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".data.MyScladContentProvider"
            android:authorities="com.example.myapplication"
            android:exported="false" />
    </application>

</manifest>